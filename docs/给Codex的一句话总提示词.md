# 给 Codex 的一句话总提示词

请基于当前目录继续完善一个“OKX 永续合约多周期形态筛选助手”，保持单页面前后端分离实现，接入 OKX v5 公共市场数据接口自动获取 `SWAP` 永续合约并对 `15m / 1H / 2H / 3H / 4H` 做 `MA5 与 MA20 收拢 + MA5 抬头向上 + K 线上穿 MA5` 规则筛选，所有结果统一展示在一张表中、每行可查看叠加 `MA5 / MA20` 的 K 线图，并支持手动刷新、每 15 分钟自动监控、微信 / 钉钉 / 通用 Webhook 去重提醒，不要实现任何自动交易或私有下单接口，同时补齐 README、`.env.example`、接口文档和测试。
